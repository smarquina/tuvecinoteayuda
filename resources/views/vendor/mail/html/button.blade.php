<div style="text-align: center; word-break: break-all; padding: 25px 0">
    <a href="{{ $url }}" class="button button-{{ $color ?? 'primary' }}"
       target="_blank" rel="noopener">{{ $slot }}</a>
</div>
